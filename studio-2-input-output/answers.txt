1. Whenever I type in cat, it repeats whatever I say until the EOF. When I type in cat < mars.txt, it prints the file contents and ends.
2. When I do cat < mars.txt > new.txt and cat new.txt, new.txt becomes a copy of mars.txt and nothing prints during the redirect command.
3. When I do cat > new.txt and write a couple of lines, the lines replace the old file and print the new lines I wrote. 
4. >> appends to the end instead of replacing the file contents. 
5. #include <unistd.h>
6. Read() return type is ssize. The sepcific return value that indicates the end of a file is 0. 
7. The behavior does match.
8. Modifying bufferSize does not affect the correctness of the program if the program writes exactly the number of bytes returned by read(). I tested 1, 5, 50, and 200 for the buffersize and in all cases, the program produced identical output to cat.
9. The tradeoff between a small bufferSize and a large bufferSize is performance versus system call overhead.